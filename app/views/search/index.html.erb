<div class="container">

    <div class="bs-docs-section">
        <div class="row">
          <div class="col-lg-12">
            <div class="page-header">
              <h1 id="tables"><%= @stationName %>駅</h1>
            </div>
          
            <h2 id="nav-tabs"></h2>
            <div class="bs-component">
              <table class="table table-striped table-hover ">
                <thead>
                  <tr>
                    <th class="col-lg-2"></th>
                    <th>概要</th>
                  </tr>
                </thead>
                <tbody>
                  <% @facility.each do |fa| %>
                  <tr>
                    <td><%= fa["Name"] %></td>
                    <td><%= simple_format(h(fa["Comment"])) %></td>
                  </tr>
                  <% end %>
                </tbody>
              </table>
            </div><!-- /example -->
          </div>
        </div>
    </div>
</div>